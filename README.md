# Reserve Standard site — polished section-lock final

This bundle is ready to publish as a static site.

## What changed
- restored the cleaner white business design direction
- removed the floating section scroll box
- added desktop-only one-scroll, one-section, hard-settle behavior
- slowed the transition and extended the post-settle lock so each section lands more deliberately
- kept mobile and smaller screens on normal scrolling
- kept the brand fully set to Reserve Standard / Reserve Standard LLC
- left the live domain unset with editable placeholders

## Update before launch
- `your-domain.com`
- `hello@your-domain.com`
- rename `CNAME.example` to `CNAME` once the final domain is set

## Notes
- desktop uses a locked chapter viewport under the header
- one wheel or trackpad gesture advances a single major section and settles there
- the form opens a prefilled email draft rather than posting to a backend
