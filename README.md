# Reserve Standard site — polished section-lock final

This bundle is ready to publish as a static site.

## What changed
- removed the floating section rail / scroll box
- slowed desktop section transitions and strengthened section lock
- kept desktop one-scroll-one-section behavior
- tightened the homepage back toward the cleaner white business design
- simplified visible contact copy so the placeholder domain does not dominate the page

## Update before launch
- `your-domain.com`
- `hello@your-domain.com`
- rename `CNAME.example` to `CNAME` once the final domain is set

## Notes
- desktop uses locked section transitions
- mobile and smaller screens use normal scrolling
- the form opens a prefilled email draft rather than posting to a backend
