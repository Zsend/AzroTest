# AZRO Systems — Polished Static Site

This is a polished, production-ready static website for the **AZRO Systems XRP Top/Bottom Detector**.

## Quick edits you may want

- **Price**: `pricing.html` (search for `$200`).
- **Version badge**: `index.html` and `pricing.html` (search for `v1.0.9`).
- **Support email / company details**: `resources.html` (Support section).
- **Canonical domain**: all HTML files have `https://azrosystems.com/...`.
  - If you publish elsewhere, update the `<link rel="canonical">` values and `sitemap.xml`.

## Deployment

- Any static host works (GitHub Pages, Cloudflare Pages, Netlify, Vercel static, etc.).
- The folder already includes:
  - `robots.txt`
  - `sitemap.xml`
  - `404.html`

## Notes

- The background art is `coolback.webp`.
- The main hero image is `chart.*`.
- Documentation PDFs live in `/pdfs`.
