
(function(){
  const doc=document.documentElement, body=document.body;
  document.querySelectorAll('[data-year]').forEach(n=>{n.textContent=new Date().getFullYear()});
  const navToggle=document.querySelector('[data-nav-toggle]'), nav=document.querySelector('[data-site-nav]');
  const closeNav=()=>{if(!nav||!navToggle)return;nav.classList.remove('is-open');navToggle.setAttribute('aria-expanded','false');body.classList.remove('has-menu-open')};
  if(nav&&navToggle){navToggle.addEventListener('click',e=>{e.stopPropagation();const open=!nav.classList.contains('is-open');nav.classList.toggle('is-open',open);navToggle.setAttribute('aria-expanded',open?'true':'false');body.classList.toggle('has-menu-open',open)});nav.addEventListener('click',e=>{if(e.target.closest('a'))closeNav()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeNav()});}
  const reviewForm=document.getElementById('review-form');
  if(reviewForm){reviewForm.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(reviewForm), name=(d.get('name')||'').trim(), business=(d.get('business')||'').trim(), email=(d.get('email')||'').trim(), website=(d.get('website')||'').trim(), industry=(d.get('industry')||'').trim(), surplus=(d.get('surplus')||'').trim(), objective=(d.get('objective')||'').trim(), status=reviewForm.querySelector('.form-status');if(!name||!business||!email){if(status)status.textContent='Please complete your name, business, and email before continuing.';return;}const inbox=(window.RS_CONFIG&&window.RS_CONFIG.reviewInbox)||'[REDACTED_EMAIL]';const subject=`Bitcoin Treasury Review — ${business}`;const bodyLines=['Reserve Standard — Bitcoin Treasury Review Request','',`Name: ${name}`,`Business: ${business}`,`Email: ${email}`,`Website: ${website||'N/A'}`,`Industry: ${industry||'N/A'}`,`Monthly treasury range: ${surplus||'N/A'}`,'','Main objective / current treasury challenge:',objective||'N/A','','Submitted from the Reserve Standard site.'];if(status)status.textContent='Opening your email client now. If nothing happens, use the direct email link below the form.';window.location.href=`mailto:${encodeURIComponent(inbox)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(bodyLines.join('\n'))}`})}
  const reveal=[...document.querySelectorAll('.hero-copy > *,.hero-stack > *,.section-intro,.copy-block,.stack-card,.matrix-shell,.band-card,.proof-panel,.proof-sidebar,.process-card,.doc-showcase,.review-launch-card,.final-cta,.review-hero > *,.review-side > *,.review-form-shell > *,.article-hero > *,.article > *,.side-card,.cover-frame,.legal-wrap > *,.footer-grid > *,.footer-base > *')];
  if('IntersectionObserver' in window && !matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver((entries,o)=>{entries.forEach(en=>{if(en.isIntersecting){en.target.classList.add('is-visible');o.unobserve(en.target)}})},{threshold:.16,rootMargin:'0px 0px -8% 0px'});reveal.forEach((n,i)=>{n.setAttribute('data-reveal','');n.style.transitionDelay=`${(i%6)*60}ms`;io.observe(n)})}else{reveal.forEach(n=>{n.setAttribute('data-reveal','');n.classList.add('is-visible')})}
  const chapterLinks=[...document.querySelectorAll('.chapter-rail [data-rail-target]')];
  const sections=chapterLinks.map(link=>({link,el:document.getElementById(link.getAttribute('data-rail-target'))})).filter(x=>x.el);
  let ticking=false;const update=()=>{ticking=false;const scrollable=Math.max(1,doc.scrollHeight-innerHeight);doc.style.setProperty('--page-progress',Math.max(0,Math.min(1,scrollY/scrollable)).toFixed(4));let active=-1,best=Infinity;sections.forEach((s,i)=>{const r=s.el.getBoundingClientRect(), dist=Math.abs((r.top+r.height/2)-innerHeight/2);if(dist<best){best=dist;active=i}});sections.forEach((s,i)=>s.link.classList.toggle('is-active',i===active));};addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}},{passive:true});update();
})();
