document.addEventListener("DOMContentLoaded",()=>{const container=document.getElementById("canvas-container");if(!container)return;const canvas=document.createElement("canvas");container.appendChild(canvas);const ctx=canvas.getContext("2d",{alpha:false});if(!ctx)return;let w=1;let h=1;let dpr=1;let active=false;let locked=false;let x=0;let y=0;let rafPending=false;let flash=null;const isCoarse=(window.matchMedia&&window.matchMedia("(hover: none) and (pointer: coarse)").matches)||(navigator.maxTouchPoints||0)>0;const joinBtn=document.querySelector(".subscribe-form button");let desktopGlowTO=0;let mobileReady=false;const clamp=(n,min,max)=>Math.max(min,Math.min(n,max));const clearBtnVars=()=>{if(!joinBtn)return;joinBtn.style.setProperty("--jgo","0");joinBtn.style.setProperty("--jgh","0")};const updateMobileGlow=()=>{if(!isCoarse||!joinBtn)return;const should=mobileReady&&!active&&!flash;if(should)joinBtn.classList.add("btn-autopulse");else joinBtn.classList.remove("btn-autopulse");if(!should)clearBtnVars()};const resize=()=>{w=window.innerWidth||1;h=window.innerHeight||1;document.documentElement.style.setProperty("--vh",`${h*0.01}px`);dpr=Math.max(1,window.devicePixelRatio||1);canvas.width=Math.floor(w*dpr);canvas.height=Math.floor(h*dpr);canvas.style.width=`${w}px`;canvas.style.height=`${h}px`;ctx.setTransform(dpr,0,0,dpr,0,0)};const tieDye=(px,py,boost=0)=>{px=clamp(px,0,w);py=clamp(py,0,h);const hue=px/w*360;const sat=clamp(py/h*100*(1+boost*0.12),0,100);const light=50+Math.sin(px*0.05)*20+boost*10;const rad=w/2;const grad=ctx.createRadialGradient(px,py,0,px,py,rad);grad.addColorStop(0,`hsl(${hue}, ${sat}%, ${light}%)`);grad.addColorStop(0.25,`hsl(${(hue+60)%360}, ${sat}%, ${light-5}%)`);grad.addColorStop(0.5,`hsl(${(hue+120)%360}, ${sat}%, ${light-10}%)`);grad.addColorStop(0.75,`hsl(${(hue+180)%360}, ${sat}%, ${light-15}%)`);grad.addColorStop(1,`hsl(${(hue+240)%360}, ${sat}%, ${light-20}%)`);return grad};const drawBlack=()=>{ctx.fillStyle="#000";ctx.fillRect(0,0,w,h)};const drawActive=()=>{ctx.fillStyle=tieDye(x,y,0);ctx.fillRect(0,0,w,h)};const drawFlash=now=>{if(!flash)return;const t=(now-flash.start)/flash.dur;if(t>=1){flash=null;updateMobileGlow();return}const pulse=Math.sin(Math.PI*t);drawBlack();ctx.save();ctx.globalAlpha=pulse;ctx.fillStyle=tieDye(flash.x,flash.y,pulse);ctx.fillRect(0,0,w,h);ctx.restore();ctx.save();ctx.globalCompositeOperation="screen";const rad=Math.max(w,h)*0.6;const glow=ctx.createRadialGradient(flash.x,flash.y,0,flash.x,flash.y,rad);glow.addColorStop(0,`rgba(128,222,234,${0.22*pulse})`);glow.addColorStop(0.35,`rgba(79,195,247,${0.14*pulse})`);glow.addColorStop(1,"rgba(0,0,0,0)");ctx.fillStyle=glow;ctx.fillRect(0,0,w,h);ctx.restore()};const render=now=>{rafPending=false;if(flash){drawFlash(now);if(flash)schedule();else if(active)drawActive();else drawBlack();return}if(!active){drawBlack();return}drawActive()};const schedule=()=>{if(rafPending)return;rafPending=true;requestAnimationFrame(render)};const pointFromEvent=e=>{if(e.touches&&e.touches.length>0)return{x:e.touches[0].clientX,y:e.touches[0].clientY};if(typeof e.clientX==="number"&&typeof e.clientY==="number")return{x:e.clientX,y:e.clientY};return null};const activateFromEvent=e=>{if(!isCoarse&&locked)return;const pt=pointFromEvent(e);if(!pt)return;x=pt.x;y=pt.y;active=true;updateMobileGlow();schedule()};const deactivate=()=>{active=false;updateMobileGlow();schedule()};const touchMove=e=>{if(e.cancelable)e.preventDefault();activateFromEvent(e)};const startFlashAt=(px,py,dur=300)=>{flash={x:px,y:py,start:performance.now(),dur};updateMobileGlow();schedule()};const desktopGlowFromPoint=(px,py)=>{if(!joinBtn)return;const r=joinBtn.getBoundingClientRect();if(!(r.width>0&&r.height>0))return;const cx=r.left+r.width/2;const cy=r.top+r.height/2;const dist=Math.hypot(px-cx,py-cy);let t=clamp(1-dist/520,0,1);t=t*t;const nx=clamp((px-r.left)/r.width,0,1);const ny=clamp((py-r.top)/r.height,0,1);joinBtn.style.setProperty("--jx",`${(nx*100).toFixed(2)}%`);joinBtn.style.setProperty("--jy",`${(ny*100).toFixed(2)}%`);joinBtn.style.setProperty("--jgo",(t*0.26).toFixed(3));joinBtn.style.setProperty("--jgh",(t*0.12).toFixed(3));if(desktopGlowTO)clearTimeout(desktopGlowTO);desktopGlowTO=window.setTimeout(()=>{clearBtnVars();desktopGlowTO=0},220)};resize();drawBlack();window.addEventListener("resize",()=>{resize();schedule()},{passive:true});const mouseMove=e=>{if(isCoarse)return;if(locked){const pt=pointFromEvent(e);if(pt)desktopGlowFromPoint(pt.x,pt.y);return}activateFromEvent(e)};container.addEventListener("mousemove",mouseMove,{passive:true});container.addEventListener("touchstart",activateFromEvent,{passive:true});container.addEventListener("touchmove",touchMove,{passive:false});window.addEventListener("touchend",deactivate,{passive:true});window.addEventListener("click",e=>{if(isCoarse)return;if(!locked&&!active)return;locked=!locked;clearBtnVars();if(desktopGlowTO){clearTimeout(desktopGlowTO);desktopGlowTO=0}if(locked){active=false;schedule();return}const pt=pointFromEvent(e);if(pt){x=pt.x;y=pt.y}active=true;schedule()},{passive:true});const email=document.getElementById("email");if(email&&isCoarse){const flashFrom=evt=>{const r=email.getBoundingClientRect();const px=typeof evt.clientX==="number"?evt.clientX:r.left+r.width/2;const py=typeof evt.clientY==="number"?evt.clientY:r.top+r.height/2;startFlashAt(px,py,300)};email.addEventListener("touchstart",flashFrom,{passive:true});email.addEventListener("mousedown",flashFrom,{passive:true})}const lc=document.querySelector(".logo-container");setTimeout(()=>{if(lc)lc.classList.add("pulsing");mobileReady=true;updateMobileGlow()},3000)});