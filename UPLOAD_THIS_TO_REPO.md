# Upload target

Copy the **contents of this folder**—not an enclosing folder—into the root of the `zsend/AzroTest` repository.

Required repository-root paths include:

- `index.html`
- `config.js`
- `assets/`
- `.github/workflows/pages.yml`
- `.nojekyll`

Do not publish real candidate, custody, employer-contact, correction, or legal-document data while the banner says **Browser test mode**.
