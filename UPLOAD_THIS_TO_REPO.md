# Upload these contents to `zsend/AzroTest`

Copy the **contents of this folder** into the repository root.

Confirm these paths exist at root:

- `index.html`
- `proof.html`
- `config.js`
- `assets/`
- `.github/workflows/pages.yml`
- `.nojekyll`

Keep the dark **Preview: fictional data only** strip visible while `config.js` uses `mode: "local"`. Do not use browser-local mode for real candidate, employer, custody, or legal data.
