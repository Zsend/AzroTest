# Upload this folder's contents

Copy the **contents of this folder** into the root of `zsend/AzroTest`.

Confirm these paths exist at repository root:

- `index.html`
- `config.js`
- `assets/`
- `.github/workflows/pages.yml`
- `.nojekyll`

Keep the dark **Preview: fictional data only** strip visible while `config.js` uses `mode: "local"`. Do not enter real sensitive information in this mode.
