(()=>{'use strict';const q=(s,r=document)=>r.querySelector(s),qa=(s,r=document)=>[...r.querySelectorAll(s)],clamp=(n,min=0,max=1)=>Math.max(min,Math.min(max,n)),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const menu=q('[data-menu-toggle]'),nav=q('[data-site-nav]');const closeMenu=()=>{if(!menu||!nav)return;menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label','Open menu');nav.classList.remove('open');document.body.classList.remove('menu-open')};if(menu&&nav){menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Close menu':'Open menu');nav.classList.toggle('open',open);document.body.classList.toggle('menu-open',open)});qa('a',nav).forEach(a=>a.addEventListener('click',closeMenu));addEventListener('resize',()=>{if(innerWidth>860)closeMenu()});addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()})}qa('[data-year]').forEach(n=>n.textContent=String(new Date().getFullYear()));const reveals=qa('[data-reveal],[data-reveal-group]');if(!reduce&&'IntersectionObserver'in window){reveals.forEach(n=>n.classList.add(n.hasAttribute('data-reveal-group')?'reveal-group':'reveal-prep'));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -5%'});reveals.forEach(n=>io.observe(n))}else reveals.forEach(n=>n.classList.add('is-visible'));const process=q('[data-process]');let ticking=false;const update=()=>{ticking=false;if(!process)return;const r=process.getBoundingClientRect(),start=innerHeight*.76,span=Math.max(r.height-innerHeight*.28,1),p=reduce?1:clamp((start-r.top)/span);process.style.setProperty('--process-progress',p.toFixed(3));const steps=qa('[data-process-step]',process);steps.forEach((s,i)=>s.classList.toggle('is-active',p>=i/(steps.length-1)-.04))};const schedule=()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}};if(process){addEventListener('scroll',schedule,{passive:true});addEventListener('resize',schedule);update()}const counters=qa('[data-impact-key]');if(counters.length){fetch('./assets/impact.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(data=>{counters.forEach(el=>el.textContent=String(Number(data[el.dataset.impactKey]||0)));qa('[data-impact-date]').forEach(el=>el.textContent=data.lastUpdated||'Pilot baseline')}).catch(()=>{})}const clean=v=>String(v||'').replace(/\r\n?/g,'\n').trim();const format=form=>{const kind=form.dataset.kind||'Connection',lines=[`PROHIBITION CAREERS — ${kind.toUpperCase()}`,'','This private message was prepared on prohibitioncareers.com.',''];qa('[data-label]',form).forEach(f=>{const val=f.type==='checkbox'?(f.checked?'Yes':'No'):clean(f.value);if(val)lines.push(`${f.dataset.label}:`,val,'')});lines.push('I understand that Prohibition Careers does not guarantee contact, referral, an interview, or employment.');return{subject:`${kind} — Prohibition Careers`,body:lines.join('\n')}};qa('[data-email-form]').forEach(form=>{const dialog=q('[data-review-dialog]');if(!dialog)return;const text=q('[data-message-text]',dialog),mail=q('[data-mail-link]',dialog),copy=q('[data-copy-message]',dialog),status=q('[data-copy-status]',dialog),close=q('[data-dialog-close]',dialog);let latest='';form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const msg=format(form);latest=msg.body;text.textContent=msg.body;mail.href=`mailto:support@prohibitioncareers.com?subject=${encodeURIComponent(msg.subject)}&body=${encodeURIComponent(msg.body)}`;status.textContent='';dialog.showModal();close?.focus()});copy?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(latest)}catch{const t=document.createElement('textarea');t.value=latest;t.style.position='fixed';t.style.opacity='0';document.body.append(t);t.select();document.execCommand('copy');t.remove()}status.textContent='Message copied.'});close?.addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()})})})();