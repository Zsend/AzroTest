Reserve Standard v109 final publish package.

Build basis:
- Uses the last stable web-materials build before the v107 scroll-conflict CSS layer.
- Preserves the approved homepage scroll engine, card choreography, progress rail math, access gate, web materials pages, PDFs, routes, SEO, robots.txt, and sitemap.xml.
- Adds only scroll-safe footer/header polish and the white monogram glyph home link in the footer.

Publish instructions:
1. Upload the contents of this package to the hosting root as a full replacement. Do not merge with older builds.
2. Purge CDN/host/browser cache.
3. Test in a fresh private/incognito browser on desktop and iPhone Safari.
4. Verify: access gate, desktop scroll effects, Asset / Problem / System cards, Operator Materials / Next Step sequence, footer home glyph, material pages, PDFs, review form, robots.txt, and sitemap.xml.
