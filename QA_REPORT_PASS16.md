# Pass 16 QA Notes

Focus of this pass: publish reliability and asset integrity.

- Converted the standalone homepage into a proper split static build.
- Extracted CSS to `styles.css`.
- Extracted JavaScript to `script.js`.
- Added `config.js` for form backend configuration.
- Extracted embedded raster assets into `/assets`.
- Added favicon, manifest, sitemap, robots, Netlify config, redirects, success page, and 404 page.
- Kept the standalone homepage as a fallback.

Remaining visual work is design-tuning, not infrastructure: final pixel alignment against the approved mockup and optional replacement with source/layered assets when available.
