Reserve Standard site publish package V11

This pass restores the exact editorial serif direction more faithfully by prioritizing an Iowan-style oldstyle display stack, with Source Serif 4 as the cross-platform web fallback.

Publish steps:
1. Delete or replace the entire current contents of the GitHub Pages test repo folder.
2. Upload every file from this package together.
3. Do not mix these files with older CSS or assets.
4. Hard refresh the browser after deploy.
