# ReserveLast_Exact restored package

Upload the contents of this folder to the root of your GitHub Pages repo. Keep `index.html`, `style.css`, `script.js`, `.nojekyll`, and `assets/` at the repo root.

This package is a deployable recovery package based on the canonical ReserveLast_Exact site structure and Reserve Standard source rules.
